#include <bits/stdc++.h>

using namespace std;
string str;

int main()
{
    cin>>str;
    int i,cnt=0;
    for(i=0;i<str.size();i++)
    {
    if(str[i]=='H' || str[i]=='Q' || str[i]=='9')
    {
       cout<<"YES"<<endl;return 0;
    }
    }
    cout<<"NO"<<endl;
    return 0;
}

